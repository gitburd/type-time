import React from 'react'

const TestText = () => {
    const quote = { content: 'Happiness is the reward we get for living to the highest right we know. Happiness is the reward we get for living to the highest right we know. Happiness is the reward we get for living to the highest right we know. Happiness is the reward we get for living to the highest right we know. Happiness is the reward we get for living to the highest right we know. Happiness is the reward we get for living to the highest right we know. Happiness is the reward we get for living to the highest right we know. hvj kubiubiu' }
    return (
        // <div className="card">
        <div className="quote-display">{quote.content}</div>
        // </div>
    )
}

export default TestText
